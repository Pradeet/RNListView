import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingTop: 5,
    },
    image: {
        height: 100,
        width: 100,
    },
    textContainer: {
        flexDirection: 'column',
    }
})